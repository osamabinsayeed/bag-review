import React from 'react';

const Blogs = () => {
    return (
        <div>

            <h2>What is context api?</h2>
            <p>Context API: Context API is a way to pass data from a prop without passing the data manually through every level of the component tree. We can skip props drilling using this</p>
            <h2>What is semantic tag?</h2>
            <p>Semantic Tag: Semantic Tag means the html tags that describes the meaning or purpose of the tag and property of the elements inside it to both browser and the developer.</p>
        </div>
    );
};

export default Blogs;